Installing KIND in Ubuntu

sudo snap install kubectl --classic

curl -Lo ./kind https://github.com/kubernetes-sigs/kind/releases/download/v0.25.0/kind-$(uname)-amd64 chmod +x ./kind sudo mv ./kind /usr/local/bin/kind

